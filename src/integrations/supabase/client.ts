// Supabase is no longer used directly — all data goes through the secure Express backend.
// This stub keeps TypeScript happy for any remaining import references.
export const supabase = null as any;
